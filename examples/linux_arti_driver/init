#!/bin/sh
mount -t proc none /proc
mount -t sysfs none /sys
mount -t devtmpfs none /dev 2>/dev/null || true
echo "ARTI Linux guest booted"
insmod /arti_rtl_test.ko
ret=$?
if [ "$ret" -eq 0 ]; then echo "ARTI LINUX TEST COMPLETE"; else echo "ARTI LINUX TEST FAILED ($ret)"; fi
poweroff -f
